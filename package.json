{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client",

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "shubham hiwale",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.0.10"
  }
}